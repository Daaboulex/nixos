{ inputs, ... }:

# ============================================================================
# Custom Package Overlays
# ============================================================================
# This file defines custom package overlays that modify or add packages
# to the nixpkgs package set. Add local package overrides here.
#
# Example:
#   final: prev: {
#     myCustomPackage = final.callPackage ./path/to/package.nix { };
#   }

final: prev: {
}