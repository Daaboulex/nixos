# Options Reference

> Auto-generated by `scripts/generate-docs.nix`. Run `bash scripts/update-docs.sh` to regenerate.

---

### `myModules.boot.boot.consoleMode`

Console resolution mode (max, keep, or specific like 1920x1080)

- **Type:** `null or string`
- **Default:** `"max"`

### `myModules.boot.boot.enable`

Whether to enable Boot configuration.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.boot.boot.initrd.enable`

Systemd initrd for Plymouth

- **Type:** `boolean`
- **Default:** `true`

### `myModules.boot.boot.loader`

Bootloader to use

- **Type:** `one of "systemd-boot", "grub", "none"`
- **Default:** `"systemd-boot"`

### `myModules.boot.boot.plymouth.enable`

Whether to enable Plymouth boot splash.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.boot.boot.plymouth.theme`

Plymouth theme to use

- **Type:** `string`
- **Default:** `"bgrt"`

### `myModules.boot.boot.secureBoot.enable`

Whether to enable Lanzaboote secure boot.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.boot.boot.secureBoot.pkiBundle`

Path to PKI bundle

- **Type:** `absolute path`
- **Default:** `"/var/lib/sbctl"`

### `myModules.boot.hibernate.enable`

Whether to enable Hibernate support via a LUKS-encrypted swap partition.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.boot.hibernate.ramSizeGB`

Physical RAM size in GiB. Hibernate requires swap partition
to be at least this large (the full RAM image is written to
swap). Asserted at eval time as a sanity check.


- **Type:** `positive integer, meaning >0`
- **Default:** `16`

### `myModules.boot.hibernate.swapDevicePriority`

Priority for the disk swap device. Default 10 keeps it above
disabled priorities but below typical zram priority (100) so
compressed RAM is preferred under steady-state memory
pressure. Disk swap engages primarily for hibernate images.


- **Type:** `signed integer`
- **Default:** `10`

### `myModules.boot.hibernate.swapLuksUuid`

UUID of the LUKS container holding swap. Get it with
`sudo cryptsetup luksUUID /dev/<swap-partition>`.


- **Type:** `string`
- **Default:** `—`

### `myModules.boot.impermanence.enable`

Whether to enable Impermanence (erase root on every boot).

- **Type:** `boolean`
- **Default:** `false`

### `myModules.boot.impermanence.extraDirectories`

Extra system directories to persist

- **Type:** `list of (string or attribute set of anything)`
- **Default:** `[]`

### `myModules.boot.impermanence.extraFiles`

Extra system files to persist

- **Type:** `list of string`
- **Default:** `[]`

### `myModules.boot.impermanence.luksDevice`

LUKS device mapper name (e.g. cryptroot)

- **Type:** `string`
- **Default:** `"cryptroot"`

### `myModules.boot.impermanence.persistPath`

Mountpoint for the persistent BTRFS subvolume

- **Type:** `absolute path`
- **Default:** `"/persist"`

### `myModules.boot.impermanence.rollback.blankSnapshot`

Name of the read-only blank root snapshot

- **Type:** `string`
- **Default:** `"@root-blank"`

### `myModules.boot.impermanence.rollback.enable`

Enable initrd rollback service that erases root on every boot

- **Type:** `boolean`
- **Default:** `true`

### `myModules.boot.impermanence.rollback.rootSubvolume`

Name of the root BTRFS subvolume

- **Type:** `string`
- **Default:** `"@"`

### `myModules.boot.kernel.cachyos.bbr3`

Enable BBR3 TCP congestion control.

- **Type:** `null or boolean`
- **Default:** `null`

### `myModules.boot.kernel.cachyos.ccHarder`

Extra -O3 / compiler aggressiveness.

- **Type:** `null or boolean`
- **Default:** `null`

### `myModules.boot.kernel.cachyos.cpusched`

CPU scheduler flavor the CachyOS kernel is built with.

- **Type:** `null or one of "bmq", "bore", "eevdf", "hardened", "rt", "rt-bore", "sched-ext"`
- **Default:** `null`

### `myModules.boot.kernel.cachyos.hugepage`

Transparent Hugepage default.

- **Type:** `null or one of "always", "madvise", "never"`
- **Default:** `null`

### `myModules.boot.kernel.cachyos.hzTicks`

Timer frequency (HZ).

- **Type:** `null or one of "100", "250", "300", "500", "750", "1000"`
- **Default:** `null`

### `myModules.boot.kernel.cachyos.kcfi`

Kernel Control Flow Integrity.

- **Type:** `null or boolean`
- **Default:** `null`

### `myModules.boot.kernel.cachyos.performanceGovernor`

Default to the `performance` cpufreq governor at boot.

- **Type:** `null or boolean`
- **Default:** `null`

### `myModules.boot.kernel.cachyos.preemptType`

Kernel preemption model.

- **Type:** `null or one of "none", "voluntary", "server", "lazy", "full", "rt"`
- **Default:** `null`

### `myModules.boot.kernel.cachyos.tickrate`

Tickless mode.

- **Type:** `null or one of "periodic", "idle", "full"`
- **Default:** `null`

### `myModules.boot.kernel.channel`

CachyOS channel selector (ignored for non-CachyOS variants).
`rc` disables LTO + per-arch variants.


- **Type:** `one of "latest", "lts", "rc"`
- **Default:** `"latest"`

### `myModules.boot.kernel.enable`

Whether to enable custom kernel variant selection.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.boot.kernel.extraParams`

Extra kernel cmdline parameters to append.

- **Type:** `list of string`
- **Default:** `[]`

### `myModules.boot.kernel.mArch`

Microarchitecture for CachyOS kernel variants. Ignored for stock
variants. Use `none` to get the CachyOS generic build.

When left as `null`, defaults to the `x86-64-v{N}` generic build
matching `myModules.host.tier` (v2 → x86-64-v2, etc.). Hosts
that want a uarch-specific compile target (e.g. `ZEN5` on Zen 5
hardware) set this explicitly.


- **Type:** `null or one of "x86-64-v2", "x86-64-v3", "x86-64-v4", "v2", "v3", "v4", "ZEN4", "ZEN5", "zen4", "none"`
- **Default:** `null`

### `myModules.boot.kernel.variant`

Kernel variant. Stock nixpkgs variants are resolved from a lookup
table; CachyOS variants go through the CachyOS override path and
pick up `channel` + `mArch` + `cachyos.*` sub-options.

**Ivy Bridge (x86-64-v2) compatibility**: CachyOS kernels
default to x86-64-v3 (AVX2 required) since the 2025 v3 bump.
Setting `mArch = "x86-64-v2"` routes to the v2-compiled
variant. Upstream `cachyos-settings-nix` publishes both
`linux-cachyos-lts-lto-x86_64-v2` and `linux-cachyos-latest-
lto-x86_64-v2` — both Ivy-Bridge safe. `cachyos-rc` has no
per-arch variants (channel=rc short-circuits the suffix).


- **Type:** `one of "default", "hardened", "latest", "lqx", "rt", "rt-latest", "xanmod", "xanmod-latest", "xanmod-lts", "xanmod-stable", "zen", "cachyos", "cachyos-lto", "cachyos-sched-ext", "cachyos-bore", "cachyos-lts", "cachyos-server", "cachyos-rt", "cachyos-hardened"`
- **Default:** `"default"`

### `myModules.desktop.displays.enable`

Whether to enable declarative display configuration.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.desktop.displays.monitors`

Monitor definitions

- **Type:** `lazy attribute set of (submodule)`
- **Default:** `{ … }`

### `myModules.desktop.displays.phantomUuids`

Stale monitor UUIDs to purge from tiling config

- **Type:** `list of string`
- **Default:** `[]`

### `myModules.desktop.flatpak.enable`

Whether to enable Flatpak support.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.desktop.plasma.ddcBrightness`

i2c for PowerDevil DDC brightness control

- **Type:** `boolean`
- **Default:** `false`

### `myModules.desktop.plasma.enable`

Whether to enable KDE Plasma Desktop Environment.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.desktop.plasma.xkbLayout`

XKB keyboard layout (e.g. 'us', 'de', 'us,de')

- **Type:** `string`
- **Default:** `"us"`

### `myModules.desktop.plasma.xkbVariant`

XKB keyboard variant

- **Type:** `string`
- **Default:** `""`

### `myModules.diagnostics.turbostat.enable`

Whether to enable turbostat CPU diagnostics tool.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.gaming.gamemode.desiredgov`

CPU governor to set when a game starts (performance = aggressive EPP hint on amd_pstate)

- **Type:** `string`
- **Default:** `"performance"`

### `myModules.gaming.gamemode.enable`

Whether to enable Gamemode CPU/GPU optimisation daemon.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.gaming.gamemode.gpuDevice`

GPU device index for gamemode optimizations (0 = first GPU)

- **Type:** `signed integer`
- **Default:** `0`

### `myModules.gaming.gamemode.gpuPerformanceLevel`

AMDGPU power_dpm_force_performance_level (null = don't set, auto = driver decides, high = max clocks)

- **Type:** `null or one of "auto", "low", "high"`
- **Default:** `null`

### `myModules.gaming.gamemode.ioprio`

IO priority for game processes (off = disabled to avoid ananicy-cpp conflict, or 0-7)

- **Type:** `string`
- **Default:** `"off"`

### `myModules.gaming.gamemode.pinCores`

Pin game to specific cores (yes = auto-detect, or core list like 0-7,16-23, no = disabled)

- **Type:** `string`
- **Default:** `"no"`

### `myModules.gaming.gamemode.renice`

Renice priority for gamemode-managed processes (0 = disabled, avoids conflict with ananicy-cpp)

- **Type:** `signed integer`
- **Default:** `0`

### `myModules.gaming.gamemode.x3dMode.default`

X3D V-Cache CCD mode when not gaming (restored on exit)

- **Type:** `null or one of "cache", "frequency"`
- **Default:** `null`

### `myModules.gaming.gamemode.x3dMode.desired`

X3D V-Cache CCD mode when gaming (cache = prefer V-Cache CCD, frequency = prefer high-clock CCD)

- **Type:** `null or one of "cache", "frequency"`
- **Default:** `null`

### `myModules.gaming.gamescope.enable`

Whether to enable Gamescope compositor.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.gaming.rocksmith.enable`

Whether to enable Rocksmith 2014 with WineASIO and RS_ASIO.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.gaming.steam.enable`

Whether to enable Steam with steam-devices (Proton via ProtonPlus).

- **Type:** `boolean`
- **Default:** `false`

### `myModules.gaming.steam.gamescope`

Gamescope session for Steam

- **Type:** `boolean`
- **Default:** `true`

### `myModules.hardware.acpid.enable`

Whether to enable ACPI event daemon.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.hardware.bluetooth.enable`

Whether to enable Bluetooth configuration.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.hardware.bluetooth.powerOnBoot`

Power on Bluetooth controller on boot

- **Type:** `boolean`
- **Default:** `false`

### `myModules.hardware.coolercontrol.enable`

Whether to enable CoolerControl fan and cooling device management.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.hardware.core.enable`

Whether to enable Core hardware configuration (firmware, microcode).

- **Type:** `boolean`
- **Default:** `false`

### `myModules.hardware.cpuAmd.enable`

Whether to enable AMD CPU optimizations.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.hardware.cpuAmd.kvm.enable`

KVM-AMD virtualization support

- **Type:** `boolean`
- **Default:** `true`

### `myModules.hardware.cpuAmd.prefcore.enable`

AMD Preferred Core technology

- **Type:** `boolean`
- **Default:** `true`

### `myModules.hardware.cpuAmd.pstate.enable`

AMD P-State driver for modern power management

- **Type:** `boolean`
- **Default:** `true`

### `myModules.hardware.cpuAmd.pstate.mode`

AMD P-State mode (active recommended for Zen 3+)

- **Type:** `one of "active", "passive", "guided"`
- **Default:** `"active"`

### `myModules.hardware.cpuAmd.updateMicrocode`

Update AMD CPU microcode

- **Type:** `boolean`
- **Default:** `true`

### `myModules.hardware.cpuAmd.x3dVcache.enable`

AMD 3D V-Cache optimizer (for dual-CCD X3D processors like 9950X3D/9900X3D)

- **Type:** `boolean`
- **Default:** `false`

### `myModules.hardware.cpuAmd.x3dVcache.mode`

3D V-Cache scheduling preference:
- "cache": prefer CCD with larger L3 cache (gaming, cache-sensitive workloads)
- "frequency": prefer CCD with higher clocks (productivity, compilation)
Requires BIOS CPPC option set to "Driver".


- **Type:** `one of "cache", "frequency"`
- **Default:** `"cache"`

### `myModules.hardware.cpuIntel.enable`

Whether to enable Intel CPU optimizations.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.hardware.cpuIntel.governor`

CPU frequency governor (powersave recommended for laptops with P-State)

- **Type:** `one of "performance", "powersave", "schedutil", "ondemand", "conservative"`
- **Default:** `"powersave"`

### `myModules.hardware.cpuIntel.iommu.enable`

Intel IOMMU (VT-d) for device passthrough

- **Type:** `boolean`
- **Default:** `false`

### `myModules.hardware.cpuIntel.kvm.enable`

KVM-Intel virtualization support (VT-x)

- **Type:** `boolean`
- **Default:** `true`

### `myModules.hardware.cpuIntel.pstate.enable`

Intel P-State driver for modern power management

- **Type:** `boolean`
- **Default:** `true`

### `myModules.hardware.cpuIntel.pstate.mode`

Intel P-State mode (active recommended for Haswell+)

- **Type:** `one of "active", "passive", "no_hwp"`
- **Default:** `"active"`

### `myModules.hardware.cpuIntel.updateMicrocode`

Update Intel CPU microcode

- **Type:** `boolean`
- **Default:** `true`

### `myModules.hardware.goxlr.enable`

Whether to enable GoXLR Mini support.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.hardware.goxlr.installProfiles`

Install custom GoXLR UCM profiles

- **Type:** `boolean`
- **Default:** `true`

### `myModules.hardware.goxlr.isMini`

Apply GoXLR Mini UCM patch

- **Type:** `boolean`
- **Default:** `true`

### `myModules.hardware.goxlr.utility.enable`

goxlr-utility daemon

- **Type:** `boolean`
- **Default:** `true`

### `myModules.hardware.gpuAmd.disableHDCP`

Disable HDCP (amdgpu.dc_hdcp_enable=0) — fixes handshake bugs on RDNA 4

- **Type:** `boolean`
- **Default:** `false`

### `myModules.hardware.gpuAmd.drmDebug`

DRM debug logging (drm.debug=0x1e) for diagnosing display black screens

- **Type:** `boolean`
- **Default:** `false`

### `myModules.hardware.gpuAmd.enable`

Whether to enable AMD Graphics configuration.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.hardware.gpuAmd.enablePPFeatureMask`

AMD GPU power management features with overdrive (ppfeaturemask=0xffff7fff — kernel default + overdrive, GFXOFF excluded)

- **Type:** `boolean`
- **Default:** `true`

### `myModules.hardware.gpuAmd.initrd.enable`

Load amdgpu in initrd (required for Plymouth)

- **Type:** `boolean`
- **Default:** `true`

### `myModules.hardware.gpuAmd.lact.enable`

LACT daemon for AMD GPU control/overclocking

- **Type:** `boolean`
- **Default:** `true`

### `myModules.hardware.gpuAmd.openCL`

OpenCL support via RustiCL (Mesa) radeonsi driver

- **Type:** `boolean`
- **Default:** `true`

### `myModules.hardware.gpuAmd.rdna4Fixes`

Apply RDNA 4 (GFX12) stability kernel params: disable scatter-gather display and GFX OFF

- **Type:** `boolean`
- **Default:** `true`

### `myModules.hardware.gpuAmd.vulkanDeviceId`

Vulkan device vendor:device ID for MESA_VK_DEVICE_SELECT (forces discrete GPU on dual-AMD systems)

- **Type:** `null or string`
- **Default:** `null`

### `myModules.hardware.gpuAmd.vulkanDeviceName`

Vulkan device name substring for DXVK_FILTER_DEVICE_NAME and VKD3D_FILTER_DEVICE_NAME (forces dGPU for translated DX9-12 games)

- **Type:** `null or string`
- **Default:** `null`

### `myModules.hardware.gpuIntel.enable`

Whether to enable Intel Graphics (i915) configuration.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.hardware.gpuIntel.kernelParams.enableDc`

Display C-states (DC) — deeper power saving states

- **Type:** `boolean`
- **Default:** `false`

### `myModules.hardware.gpuIntel.kernelParams.enableFbc`

Frame Buffer Compression (FBC) — reduces power consumption

- **Type:** `boolean`
- **Default:** `false`

### `myModules.hardware.gpuIntel.kernelParams.enablePsr`

Panel Self Refresh (PSR) — may cause flickering on some displays

- **Type:** `boolean`
- **Default:** `false`

### `myModules.hardware.gpuIntel.openCL`

OpenCL support via RustiCL (Mesa) iris driver

- **Type:** `boolean`
- **Default:** `true`

### `myModules.hardware.graphics.enable`

Whether to enable Graphics support.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.hardware.graphics.enable32Bit`

32-bit graphics support

- **Type:** `boolean`
- **Default:** `true`

### `myModules.hardware.graphics.mesaGit.drivers`

GPU vendors to compile drivers for. Only the selected vendor drivers
plus common essentials (llvmpipe, zink, virgl, swrast) are built.

Use multiple entries for multi-GPU setups (e.g. Intel iGPU + NVIDIA dGPU).
An empty list (default) builds all drivers.


- **Type:** `list of (one of "amd", "intel", "nvidia")`
- **Default:** `[]`

### `myModules.hardware.graphics.mesaGit.enable`

Whether to enable mesa-git (bleeding-edge) instead of nixpkgs mesa.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.hardware.graphics.openCL.rusticlDrivers`

Gallium drivers to enable in RustiCL (Mesa's OpenCL implementation).
GPU vendor modules append their driver automatically when their openCL
option is enabled. Set by gpu-amd (radeonsi) and gpu-intel (iris).
Assembled into RUSTICL_ENABLE session variable as a comma-separated list.


- **Type:** `list of string`
- **Default:** `[]`

### `myModules.hardware.networking.dnsOverTls`

systemd-resolved DNSOverTLS policy. "true" requires DoT and refuses
plaintext fallback (recommended). "opportunistic" tries DoT first,
falls back to plaintext on failure (weaker guarantee but more
robust on captive portals). "false" disables DoT entirely.


- **Type:** `one of "true", "opportunistic", "false"`
- **Default:** `"true"`

### `myModules.hardware.networking.enable`

Whether to enable Networking configuration.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.hardware.networking.nameservers`

DNS nameservers. Default is Mullvad's adblock DoT resolver with
SNI hint so systemd-resolved validates the TLS certificate.
Override per-host if you want a different upstream.


- **Type:** `list of string`
- **Default:** `["194.242.2.3#dns.mullvad.net" "2a07:e340::3#dns.mullvad.net"]`

### `myModules.hardware.networking.openPortRanges`

Port ranges to open on TCP and UDP (e.g. `[{ from = 1000; to = 2000; }]`)

- **Type:** `list of (submodule)`
- **Default:** `[]`

### `myModules.hardware.networking.openPorts`

List of TCP ports to open

- **Type:** `list of signed integer`
- **Default:** `[]`

### `myModules.hardware.pipewire.enable`

Whether to enable Audio configuration with PipeWire.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.hardware.pipewire.extraLadspaPackages`

LADSPA plugin packages forwarded to services.pipewire.extraLadspaPackages

- **Type:** `list of package`
- **Default:** `[]`

### `myModules.hardware.pipewire.lowLatency`

Whether to enable Low latency configuration (48kHz, quantum 256).

- **Type:** `boolean`
- **Default:** `false`

### `myModules.hardware.pipewire.quantum`

Audio buffer size in samples (lower = less latency, more CPU). 256=5.3ms, 512=10.7ms, 1024=21ms

- **Type:** `signed integer`
- **Default:** `256`

### `myModules.hardware.power.enable`

Whether to enable Power management configuration.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.hardware.power.laptop`

Whether to enable Laptop power optimizations (TLP).

- **Type:** `boolean`
- **Default:** `false`

### `myModules.hardware.udevAccess.debuggingProbes`

Embedded debugging probes (LPC-Link2, ESP32) udev rules

- **Type:** `boolean`
- **Default:** `false`

### `myModules.hardware.udevAccess.enable`

Whether to enable USB device access rules for development hardware.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.hardware.udevAccess.saleae`

Saleae Logic analyzer udev rules

- **Type:** `boolean`
- **Default:** `false`

### `myModules.hardware.upower.enable`

Whether to enable UPower (battery/power monitoring).

- **Type:** `boolean`
- **Default:** `false`

### `myModules.hardware.usbPower.enable`

Whether to enable USB device power management fix (prevents WiFi drops).

- **Type:** `boolean`
- **Default:** `false`

### `myModules.hardware.usbPower.vendorId`

USB vendor ID to disable power management for (default: Realtek).

- **Type:** `string`
- **Default:** `"0bda"`

### `myModules.hardware.usbmuxd.enable`

Whether to enable USB multiplexing daemon (iOS device support).

- **Type:** `boolean`
- **Default:** `false`

### `myModules.host.tier`

CPU microarchitecture tier:
  - `v2` — x86-64-v2 (SSE4.2, no AVX2). Ivy Bridge / older AMD.
  - `v3` — x86-64-v3 (AVX2 + BMI2). Haswell+ / Zen+.
  - `v4` — x86-64-v4 (AVX-512). Zen4+ / Sapphire Rapids+.
Consumed by kernel.nix as mArch default (see
`parts/boot/kernel.nix`).


- **Type:** `one of "v2", "v3", "v4"`
- **Default:** `—`

### `myModules.input.duckyOneXMini.board.product`

USB product ID for the keyboard board HID interface

- **Type:** `string`
- **Default:** `"001d"`

### `myModules.input.duckyOneXMini.enable`

Whether to enable Ducky One X Mini keyboard HID access (udev rules for VIA/Vial).

- **Type:** `boolean`
- **Default:** `false`

### `myModules.input.duckyOneXMini.mcu.product`

USB product ID for the keyboard MCU HID interface

- **Type:** `string`
- **Default:** `"0021"`

### `myModules.input.duckyOneXMini.vendor`

USB vendor ID for the Ducky keyboard

- **Type:** `string`
- **Default:** `"3233"`

### `myModules.input.libinput.enable`

libinput touchpad with natural scrolling and tap-to-click

- **Type:** `boolean`
- **Default:** `true`

### `myModules.input.libinput.naturalScrolling`

Natural (reverse) scrolling direction

- **Type:** `boolean`
- **Default:** `true`

### `myModules.input.libinput.tapping`

Tap-to-click

- **Type:** `boolean`
- **Default:** `true`

### `myModules.input.ratbagd.enable`

Whether to enable Piper mouse configuration tool and ratbagd service.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.input.streamcontroller.enable`

Whether to enable StreamController (Elgato Stream Deck).

- **Type:** `boolean`
- **Default:** `false`

### `myModules.input.yeetmouse.devices.g502.dpi`

Mouse DPI setting (reported to libinput via HWDB)

- **Type:** `signed integer`
- **Default:** `1600`

### `myModules.input.yeetmouse.devices.g502.enable`

Whether to enable Libinput flat acceleration profile for Logitech G502 (prevents double acceleration).

- **Type:** `boolean`
- **Default:** `false`

### `myModules.input.yeetmouse.devices.g502.pollingRate`

Mouse polling rate in Hz

- **Type:** `signed integer`
- **Default:** `1000`

### `myModules.input.yeetmouse.devices.g502.wiredProductId`

USB product ID for the wired G502

- **Type:** `string`
- **Default:** `"c08d"`

### `myModules.input.yeetmouse.devices.g502.wirelessProductId`

USB product ID for the Lightspeed Receiver

- **Type:** `string`
- **Default:** `"c539"`

### `myModules.input.yeetmouse.enable`

Whether to enable YeetMouse kernel mouse acceleration driver.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.macbook.hidApple.enable`

Apple keyboard hid_apple configuration (fnMode, Option/Command swap)

- **Type:** `boolean`
- **Default:** `true`

### `myModules.macbook.hidApple.fnMode`

Apple keyboard fn key behavior (0=disabled, 1=press fn for media, 2=press fn for F-keys)

- **Type:** `one of 0, 1, 2`
- **Default:** `2`

### `myModules.macbook.hidApple.swapOptCmd`

Swap Option and Command keys (makes Cmd act as Alt)

- **Type:** `boolean`
- **Default:** `true`

### `myModules.macbook.mbpfan.enable`

mbpfan daemon for MacBook fan control

- **Type:** `boolean`
- **Default:** `true`

### `myModules.macbook.mbpfan.highTemp`

Temperature for high fan speed (Celsius)

- **Type:** `signed integer`
- **Default:** `65`

### `myModules.macbook.mbpfan.lowTemp`

Temperature to start ramping fan (Celsius)

- **Type:** `signed integer`
- **Default:** `45`

### `myModules.macbook.mbpfan.maxTemp`

Maximum temperature before full fan (Celsius)

- **Type:** `signed integer`
- **Default:** `80`

### `myModules.macbook.mbpfan.pollingInterval`

Fan polling interval in seconds

- **Type:** `signed integer`
- **Default:** `1`

### `myModules.macbook.patches.enable`

Whether to enable MacBook kernel patches (AppleSMC fixes, AT24 warning suppression).

- **Type:** `boolean`
- **Default:** `false`

### `myModules.macbook.wifi.enable`

Whether to enable Broadcom BCM4331 WiFi via b43 driver (MacBook Pro 9,2).

- **Type:** `boolean`
- **Default:** `false`

### `myModules.nix.nix.enable`

Whether to enable Nix daemon configuration and settings.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.nix.nix.githubTokenSource`

Where nix-daemon gets the GitHub API token (for rate-limited
flake fetches).
  • `gh-cli` — activation script reads `gh auth token` into
    /etc/nix/github-token. Opportunistic (no-op if gh not
    configured for root).
  • `none` — no GitHub token configured. Flake updates hit
    unauthenticated rate limits.


- **Type:** `one of "gh-cli", "none"`
- **Default:** `"gh-cli"`

### `myModules.nix.nixLd.enable`

Whether to enable nix-ld for running unpatched binaries.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.nix.nixLd.libraries`

Libraries for nix-ld to provide to unpatched binaries

- **Type:** `list of package`
- **Default:** `["/nix/store/sanx9fg8mry8mq92zhlm5qvb83qlxrlx-gcc-15.2.0" "/nix/store/ixhlv41i2wpl84xgjcks061dz4yssbg3-zlib-1.3.2" "/nix/store/82rcfg87mdxlkgywb2y4pm4q7az3qwap-fuse-3.17.4-bin" "/nix/store/0dmcpvbp2rvz8a7p2yhsrkpcb41kr6wl-icu4c-76.1" "/nix/store/0wnl2h5xgy1q7bgkqdbiyzxnrq3cmigi-nss-3.112.3" "/nix/store/2di90l89y2ygdy3rbws7dhg9nrvd3pnx-openssl-3.6.1-bin" "/nix/store/sm2nq18jjqp4x0sxpl6lrvwl9rx6mvj2-curl-8.19.0-bin" "/nix/store/pa6n8nrmgq8jswk2pkrl5qprcls1r0ch-expat-2.7.5"]`

### `myModules.nix.remoteBuilder.client.enable`

Whether to enable offload builds to a remote nix-daemon over SSH.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.nix.remoteBuilder.client.extraHostNames`

Additional hostnames/IPs the remote answers to. Added to
the SSH known_hosts entry alongside `hostName`, so the
same host public key is trusted no matter which name was
used to connect (hostname + .local + LAN IP, for example).


- **Type:** `list of string`
- **Default:** `[]`

### `myModules.nix.remoteBuilder.client.hostName`

Remote builder hostname (SSH-reachable).

- **Type:** `string`
- **Default:** `—`

### `myModules.nix.remoteBuilder.client.hostPublicKey`

Remote builder's SSH host public key — get it from the
remote with `cat /etc/ssh/ssh_host_ed25519_key.pub`.
When set, the client pre-trusts the remote in its root
known_hosts so nix-daemon's non-interactive SSH never
hits a trust-on-first-use prompt. If left null, the
client falls back to StrictHostKeyChecking=accept-new
semantics (first connection pins the key).


- **Type:** `null or string`
- **Default:** `null`

### `myModules.nix.remoteBuilder.client.maxJobs`

Parallel derivations the remote can build concurrently.
Rule of thumb: size to remote's logical cores (physical ×
SMT). Each build internally uses `nix.settings.cores`
worker threads, so the product must fit hardware.

Examples:
  • 9950X3D (16C/32T): `maxJobs = 32` with `cores = 1`
    → 32 concurrent compiles, each single-threaded. Best
    for many-small-derivations workloads.
  • 9950X3D balanced: `maxJobs = 16` with `cores = 4`
    → 16 builds × 4 threads = 64 compile-threads. Best
    for kernel/chromium/LibreOffice.
  • Conservative default (8): works on any modern quad.

`nix.buildMachines` takes an integer here — there is no
"auto" for remotes (the remote's own `nix.settings.max-
jobs = "auto"` governs whether it accepts the submission).


- **Type:** `positive integer, meaning >0`
- **Default:** `8`

### `myModules.nix.remoteBuilder.client.speedFactor`

Relative speed vs local. Higher = nix prefers this remote
when the local machine and remote both support a given
build. 10× for ryzen-vs-MBP (roughly: Zen 5 16C/32T
~20× the compile-wall-clock of Ivy Bridge 2C/4T on GCC).


- **Type:** `positive integer, meaning >0`
- **Default:** `10`

### `myModules.nix.remoteBuilder.client.sshKey`

Path to SSH private key on THIS host for remote builder auth.

- **Type:** `string`
- **Default:** `"/root/.ssh/remotebuild"`

### `myModules.nix.remoteBuilder.client.sshUser`

SSH user on the remote builder. Must be in trusted-users there.

- **Type:** `string`
- **Default:** `"remotebuild"`

### `myModules.nix.remoteBuilder.client.staticIp`

LAN IP for the remote. When set, adds a `networking.hosts`
entry so `hostName` resolves even if mDNS breaks. Leave
null to rely entirely on mDNS/DNS resolution.


- **Type:** `null or string`
- **Default:** `null`

### `myModules.nix.remoteBuilder.client.supportedFeatures`

Build features the remote supports (needed for compilers + VM tests).

- **Type:** `list of string`
- **Default:** `["nixos-test" "benchmark" "big-parallel" "kvm"]`

### `myModules.nix.remoteBuilder.client.system`

System architecture the remote can build for.

- **Type:** `string`
- **Default:** `"x86_64-linux"`

### `myModules.nix.remoteBuilder.server.authorizedKeys`

SSH public keys of clients allowed to submit builds.
Paste output of `sudo cat /root/.ssh/remotebuild.pub` from each client here.


- **Type:** `list of string`
- **Default:** `[]`

### `myModules.nix.remoteBuilder.server.enable`

Whether to enable accept remote build requests (adds remotebuild user + trusted).

- **Type:** `boolean`
- **Default:** `false`

### `myModules.primaryUser`

Primary system username used across all modules

- **Type:** `string`
- **Default:** `"user"`

### `myModules.security.agenix.enable`

Whether to enable agenix secret management.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.security.agenix.identityPaths`

Private identity files agenix may use to decrypt host secrets.

- **Type:** `list of string`
- **Default:** `["/etc/ssh/ssh_host_ed25519_key"]`

### `myModules.security.agenix.secrets`

Encrypted agenix secrets keyed by runtime secret name. Defaults each entry to `secretsRoot/<name>.age`.

- **Type:** `attribute set of (submodule)`
- **Default:** `{ … }`

### `myModules.security.agenix.secretsRoot`

Directory containing encrypted agenix secret files.

- **Type:** `absolute path`
- **Default:** `"/nix/store/5rdmb0vb6x4yj5xzqjrs3vzabr8fa795-source/secrets"`

### `myModules.security.hardening.enable`

Whether to enable System-wide security hardening.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.security.hardening.firejail.enable`

Firejail sandboxing

- **Type:** `boolean`
- **Default:** `false`

### `myModules.security.portmaster.autostart`

Whether portmaster.service starts automatically on boot. When false, the service is installed but must be started manually with `sudo systemctl start portmaster`.

- **Type:** `boolean`
- **Default:** `true`

### `myModules.security.portmaster.enable`

Whether to enable Portmaster privacy firewall.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.security.portmaster.extraArgs`

Extra command-line arguments for portmaster-core

- **Type:** `list of string`
- **Default:** `[]`

### `myModules.security.portmaster.forceSettings`

Hard declarative overrides — applied on every preStart, UI edits are
reverted. Use for settings that MUST stay a specific value or the
system breaks (Mullvad-compat DNS flags, kill switch, resolver
listening address).


- **Type:** `attribute set of anything`
- **Default:** `{ … }`

### `myModules.security.portmaster.notifier`

Whether to enable Portmaster system tray notifier (autostart).

- **Type:** `boolean`
- **Default:** `false`

### `myModules.security.portmaster.settings`

Soft declarative settings — seeded into /var/lib/portmaster/config.json
on first boot, then UI edits win. Use for preferences the user may want
to tweak live (filter list selections, expertise level, notifications).


- **Type:** `attribute set of anything`
- **Default:** `{ … }`

### `myModules.security.portmasterMullvadCompat.enable`

Whether to enable Mullvad + Portmaster coexistence. Insert an iptables RETURN rule
at the top of PORTMASTER-INGEST-OUTPUT so Portmaster does not
overwrite Mullvad's fwmark on WireGuard-encapsulated packets.
Requires both myModules.security.portmaster.enable and an active
Mullvad daemon — the rule is harmless when Mullvad is stopped
.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.security.portmasterMullvadCompat.mark`

Mullvad's WireGuard fwmark, as hex. Userspace wireguard-go
stamps this via SO_MARK on every encapsulated UDP packet; the
matching `ip rule` entry uses `fwmark 0x6d6f6c65`. Mullvad has
shipped this value since the linux daemon gained userspace WG
support; changing it here only makes sense if Mullvad upstream
changes the constant.


- **Type:** `string`
- **Default:** `"0x6d6f6c65"`

### `myModules.security.ssh.enable`

Whether to enable Secure SSH server configuration.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.security.ssh.fail2banIgnoreIPs`

IP ranges to never ban (add your LAN/VPN subnets)

- **Type:** `list of string`
- **Default:** `["127.0.0.1/8" "::1/128"]`

### `myModules.security.ssh.trustedKeys`

List of trusted SSH public keys

- **Type:** `list of string`
- **Default:** `[]`

### `myModules.sensors.msr.enable`

Whether to enable x86 MSR access (APERF/MPERF, RAPL energy counters).

- **Type:** `boolean`
- **Default:** `false`

### `myModules.sensors.nct6775.enable`

Whether to enable Nuvoton NCT67xx Super I/O sensors (motherboard Vcore, fans, temps).

- **Type:** `boolean`
- **Default:** `false`

### `myModules.sensors.ryzenSmu.enable`

Whether to enable AMD ryzen_smu kernel module (Curve Optimizer, PBO, boost override).

- **Type:** `boolean`
- **Default:** `false`

### `myModules.sensors.zenpower.enable`

Whether to enable Zenpower5 AMD CPU sensors (replaces k10temp — Zen 1 through Zen 5).

- **Type:** `boolean`
- **Default:** `false`

### `myModules.services.avahi.enable`

Whether to enable Avahi mDNS/zeroconf daemon + NSS integration.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.services.avahi.openFirewall`

Open UDP port 5353 for mDNS multicast in the host firewall.
Required for any mDNS interaction — both advertisement AND
consumption rely on inbound multicast. Disabling this blocks
other hosts' announcements too. To stop advertising while
staying able to resolve `.local` names, set `publish = false`
and keep `openFirewall = true`.


- **Type:** `boolean`
- **Default:** `true`

### `myModules.services.avahi.publish`

Whether this host advertises itself via mDNS. Default true
so other LAN hosts can resolve us by `.local` name. Set
false on hosts that shouldn't be discoverable.


- **Type:** `boolean`
- **Default:** `true`

### `myModules.services.cups.browsing`

Run cups-browsed to auto-discover network printers.
Set false on hosts with no printers to silence periodic
Create-Printer-Subscriptions client-error-bad-request
churn in cupsd logs (cups-browsed renews stale leases
every few hours; with no targets the first Create probe
carries a short-form payload cupsd rejects before the
retry with full attrs succeeds).


- **Type:** `boolean`
- **Default:** `true`

### `myModules.services.cups.enable`

Whether to enable Printing support (CUPS).

- **Type:** `boolean`
- **Default:** `false`

### `myModules.services.earlyoom.avoidRegex`

Regex matching process names earlyoom must never kill.

- **Type:** `string`
- **Default:** `"^(sshd|systemd|Xorg|Xwayland|kwin|plasmashell|pipewire|wireplumber)$"`

### `myModules.services.earlyoom.enable`

Whether to enable Early OOM killer (prevents system freezes).

- **Type:** `boolean`
- **Default:** `false`

### `myModules.services.earlyoom.memoryThreshold`

Minimum free memory percentage before killing

- **Type:** `signed integer`
- **Default:** `5`

### `myModules.services.earlyoom.preferRegex`

Regex matching process names that earlyoom should preferentially kill
under memory pressure. Per-host overridable so low-RAM hosts (e.g.
MBP 9,2) can include nix-daemon, build compilers, and editors that
commonly trigger OOM cascades.


- **Type:** `string`
- **Default:** `"^(Web Content|Isolated Web|firefox|chromium|steam|gamescope)$"`

### `myModules.services.earlyoom.swapThreshold`

Minimum free swap percentage before killing

- **Type:** `signed integer`
- **Default:** `10`

### `myModules.services.geoclue.enable`

Whether to enable GeoClue2 location service.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.services.mullvad.enable`

Whether to enable Mullvad VPN — declarative daemon settings.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.services.mullvad.settings`

Forwarded to `services.mullvad-vpn-declarative.settings`.
See Daaboulex/mullvad-vpn-nix README for option reference.


- **Type:** `attribute set of anything`
- **Default:** `{ … }`

### `myModules.services.sunshine.adapterName`

GPU render node for hardware encoding.

- **Type:** `string`
- **Default:** `"/dev/dri/renderD128"`

### `myModules.services.sunshine.applications`

Applications exposed to Moonlight (services.sunshine.applications).

- **Type:** `attribute set of anything`
- **Default:** `{ … }`

### `myModules.services.sunshine.audioSink`

PipeWire sink to capture. Null = default monitor. Set to .monitor of your speakers to stream WHILE audio plays on desktop.

- **Type:** `null or string`
- **Default:** `null`

### `myModules.services.sunshine.enable`

Whether to enable Sunshine game streaming server (Moonlight-compatible).

- **Type:** `boolean`
- **Default:** `false`

### `myModules.services.sunshine.encoder`

Hardware encoder. AMD on Linux uses vaapi (Mesa).

- **Type:** `one of "vaapi", "nvenc", "amf", "quicksync", "software"`
- **Default:** `"vaapi"`

### `myModules.services.sunshine.extraSettings`

Additional Sunshine settings merged into services.sunshine.settings.

- **Type:** `attribute set of anything`
- **Default:** `{ … }`

### `myModules.services.sunshine.outputName`

Display index to capture (Wayland uses numeric index, check sunshine.log for 'Found display: <N>').

- **Type:** `string`
- **Default:** `"0"`

### `myModules.services.sunshine.streamAudioToClientAndHost`

If true, audio plays on desktop speakers AND streams to Moonlight (clone). If false, only streams (silent locally).

- **Type:** `boolean`
- **Default:** `true`

### `myModules.services.sunshine.virtualSink`

Virtual sink name — creates a separate sink so audio ONLY goes to stream, silent on desktop. Set to null to mirror to desktop instead.

- **Type:** `null or string`
- **Default:** `null`

### `myModules.services.syncthing.devices`

Peer devices to sync with

- **Type:** `attribute set of (submodule)`
- **Default:** `{ … }`

### `myModules.services.syncthing.enable`

Whether to enable Syncthing continuous file synchronization.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.services.syncthing.folders`

Folders to sync

- **Type:** `attribute set of (submodule)`
- **Default:** `{ … }`

### `myModules.services.syncthing.startDelay`

Seconds to delay the syncthing daemon after login. On slow SSDs
(DRAM-less, SATA), the initial full-folder scan after boot can
saturate the I/O queue and freeze the UI. A delay lets the desktop
session settle first. 0 = start immediately (default).


- **Type:** `unsigned integer, meaning >=0`
- **Default:** `0`

### `myModules.storage.btrbk.enable`

Whether to enable btrbk incremental btrfs snapshot replication.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.storage.btrbk.snapshotPreserve`

Retention on the SOURCE side (how many short-lived snapshots
kept live on the root filesystem while btrbk runs). Syntax:
"24h 14d 8w 6m" = hourly × 24, daily × 14, weekly × 8, monthly × 6.


- **Type:** `string`
- **Default:** `"24h 14d 8w 6m"`

### `myModules.storage.btrbk.sourcePath`

Mount point of the source btrfs filesystem. Usually "/".
btrbk will snapshot subvolumes accessible from here.


- **Type:** `string`
- **Default:** `"/"`

### `myModules.storage.btrbk.subvolumes`

Subvolume names to replicate. Default covers root + home +
nix store + logs. Skip @cache / @tmp / @snapshots (regenerable
or meta).


- **Type:** `list of string`
- **Default:** `["@" "@home" "@nix" "@log"]`

### `myModules.storage.btrbk.targetPath`

Mount point of the target btrfs filesystem on the second drive.
Must be a btrfs filesystem. Snapshots land in this directory.


- **Type:** `string`
- **Default:** `—`

### `myModules.storage.btrbk.targetPreserve`

Retention on the TARGET drive. Longer history here — target
exists precisely to hold deep backup history.


- **Type:** `string`
- **Default:** `"48h 30d 12w 12m"`

### `myModules.storage.btrbk.timer`

systemd OnCalendar spec for the replication timer. Default
hourly. Finer cadence (e.g. *:0/30 = every 30 min) is fine;
incremental sends are cheap.


- **Type:** `string`
- **Default:** `"hourly"`

### `myModules.storage.filesystems.enable`

Whether to enable Universal filesystem support.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.storage.filesystems.enableAll`

Master switch — when true, every category is enabled
regardless of the per-category `enable<X>` toggles. The
per-category toggles are an OR-gate, not an override:
`enableAll = false; enableLinux = true;` still enables
Linux. To disable a category, set `enableAll = false` AND
the per-category toggle false.


- **Type:** `boolean`
- **Default:** `true`

### `myModules.storage.filesystems.enableLegacy`

Legacy filesystems (ReiserFS)

- **Type:** `boolean`
- **Default:** `false`

### `myModules.storage.filesystems.enableLinux`

Linux filesystems (ext4, btrfs, xfs, f2fs)

- **Type:** `boolean`
- **Default:** `true`

### `myModules.storage.filesystems.enableMac`

macOS filesystems (HFS+)

- **Type:** `boolean`
- **Default:** `true`

### `myModules.storage.filesystems.enableOptical`

Optical disc filesystems (ISO 9660, UDF)

- **Type:** `boolean`
- **Default:** `true`

### `myModules.storage.filesystems.enableWindows`

Windows filesystems (NTFS, exFAT, FAT32)

- **Type:** `boolean`
- **Default:** `true`

### `myModules.storage.fstrim.enable`

Whether to enable Periodic SSD TRIM.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.storage.fstrim.interval`

How often to run fstrim

- **Type:** `string`
- **Default:** `"weekly"`

### `myModules.tuning.cachyos.amdgpuGcnCompat.enable`

Whether to enable CachyOS amdgpuGcnCompat.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.tuning.cachyos.audio.enable`

Whether to enable CachyOS audio.

- **Type:** `boolean`
- **Default:** `true`

### `myModules.tuning.cachyos.coredump.enable`

Whether to enable CachyOS coredump.

- **Type:** `boolean`
- **Default:** `true`

### `myModules.tuning.cachyos.debuginfod.enable`

Whether to enable CachyOS debuginfod.

- **Type:** `boolean`
- **Default:** `true`

### `myModules.tuning.cachyos.enable`

Whether to enable CachyOS system optimizations (upstream-matched settings).

- **Type:** `boolean`
- **Default:** `false`

### `myModules.tuning.cachyos.ioSchedulers.enable`

Whether to enable CachyOS ioSchedulers.

- **Type:** `boolean`
- **Default:** `true`

### `myModules.tuning.cachyos.networkManager.enable`

Whether to enable CachyOS networkManager.

- **Type:** `boolean`
- **Default:** `true`

### `myModules.tuning.cachyos.ntsync.enable`

Whether to enable CachyOS ntsync.

- **Type:** `boolean`
- **Default:** `true`

### `myModules.tuning.cachyos.nvidia.enable`

Whether to enable CachyOS nvidia.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.tuning.cachyos.storage.enable`

Whether to enable CachyOS storage.

- **Type:** `boolean`
- **Default:** `true`

### `myModules.tuning.cachyos.systemd.enable`

Whether to enable CachyOS systemd.

- **Type:** `boolean`
- **Default:** `true`

### `myModules.tuning.cachyos.thp.enable`

Whether to enable CachyOS thp.

- **Type:** `boolean`
- **Default:** `true`

### `myModules.tuning.cachyos.timesyncd.enable`

Whether to enable CachyOS timesyncd.

- **Type:** `boolean`
- **Default:** `true`

### `myModules.tuning.cachyos.zram.enable`

Whether to enable CachyOS zram.

- **Type:** `boolean`
- **Default:** `true`

### `myModules.tuning.corecycler.deviceAccess`

Whether to grant primaryUser access to MSR devices and SMU sysfs via a dedicated group and udev rules. No sudo required for monitoring and CO access.

- **Type:** `boolean`
- **Default:** `true`

### `myModules.tuning.corecycler.enable`

Whether to enable CoreCyclerLx device access (MSR, SMU sysfs, dmesg).

- **Type:** `boolean`
- **Default:** `false`

### `myModules.tuning.performance.ananicy`

Ananicy-cpp process prioritization

- **Type:** `boolean`
- **Default:** `true`

### `myModules.tuning.performance.enable`

Whether to enable Performance tuning and optimization.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.tuning.performance.governor`

CPU frequency governor (cpufreq scaling_governor). Finite set
defined by the kernel cpufreq subsystem. `powersave` is the
safe default on scx-scheduled hosts (scx handles frequency
decisions); pick `performance` for VFIO or low-latency hosts.


- **Type:** `one of "performance", "powersave", "schedutil", "ondemand", "conservative", "userspace"`
- **Default:** `"powersave"`

### `myModules.tuning.performance.irqbalance`

IRQ balancing across CPU cores

- **Type:** `boolean`
- **Default:** `true`

### `myModules.tuning.performance.scx.enable`

Whether to enable Sched-ext (scx) userspace CPU schedulers.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.tuning.performance.scx.extraArgs`

Extra arguments passed to the scheduler

- **Type:** `list of string`
- **Default:** `[]`

### `myModules.tuning.performance.scx.scheduler`

Which SCX scheduler to run

- **Type:** `one of "scx_lavd", "scx_bpfland", "scx_cosmos", "scx_rusty", "scx_rustland", "scx_flash", "scx_p2dq", "scx_beerland", "scx_mitosis", "scx_tickless", "scx_central", "scx_nest", "scx_layered"`
- **Default:** `"scx_lavd"`

### `myModules.tuning.sysctls.enable`

Whether to enable Extra performance sysctls: BBR, CAKE, tcp_fastopen, buffer sizes, max_map_count.

- **Type:** `boolean`
- **Default:** `true`

### `myModules.users.enable`

Whether to enable User configuration.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.vfio.anancyOverride`

Override CachyOS ananicy-cpp rules for QEMU. Default rules classify QEMU as Heavy_CPU (nice=9, ionice=7) which deprioritizes VM performance. This adds custom rules that give QEMU and libvirt high priority instead.

- **Type:** `boolean`
- **Default:** `true`

### `myModules.vfio.bindMethod`

static = vfio-pci.ids kernel param (GPU always captured at boot); dynamic = libvirt hooks bind/unbind on VM start/stop

- **Type:** `one of "static", "dynamic"`
- **Default:** `"dynamic"`

### `myModules.vfio.enable`

Whether to enable VFIO GPU passthrough with stealth VM management.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.vfio.evdev.enable`

Evdev input passthrough for keyboard/mouse

- **Type:** `boolean`
- **Default:** `true`

### `myModules.vfio.evdev.keyboardPath`

Path to keyboard event device (e.g. /dev/input/by-id/...)

- **Type:** `null or string`
- **Default:** `null`

### `myModules.vfio.evdev.mousePath`

Path to mouse event device (e.g. /dev/input/by-id/...)

- **Type:** `null or string`
- **Default:** `null`

### `myModules.vfio.hostCpuMask`

Hex bitmask of host-only CPUs (CCD1 cores). Used to restrict scx scheduler during VM runtime. Example: 0xff00ff00 for cores 8-15 + threads 24-31 on a dual-CCD Zen 5.

- **Type:** `string`
- **Default:** `"0xffffffff"`

### `myModules.vfio.hostGpuDriver`

Host GPU driver used for recovery rebinding when a passthrough device is stuck on vfio-pci from a prior run. Set to 'nouveau' or 'nvidia' for NVIDIA hosts, 'i915' for Intel. Defaults to 'amdgpu'.

- **Type:** `string`
- **Default:** `"amdgpu"`

### `myModules.vfio.hugepages.count`

Number of hugepages to allocate

- **Type:** `signed integer`
- **Default:** `8192`

### `myModules.vfio.hugepages.enable`

Hugepage allocation for VM memory (allocated on VM start, freed on VM stop)

- **Type:** `boolean`
- **Default:** `false`

### `myModules.vfio.hugepages.size`

Hugepage size (1G = fewer TLB misses, best for gaming VMs)

- **Type:** `one of "2M", "1G"`
- **Default:** `"2M"`

### `myModules.vfio.kvmfr.enable`

KVMFR shared memory for Looking Glass frame relay

- **Type:** `boolean`
- **Default:** `true`

### `myModules.vfio.kvmfr.memoryMB`

KVMFR shared memory size in MB (32=1440p SDR, 64=4K SDR, 128=4K HDR)

- **Type:** `signed integer`
- **Default:** `64`

### `myModules.vfio.machineType`

QEMU machine type for VM definitions

- **Type:** `string`
- **Default:** `"pc-q35-10.0"`

### `myModules.vfio.restrictScxToHost`

Restrict the scx scheduler to host-only cores while a VM is running. When true, the hook restarts scx_bpfland with --primary-domain set to hostCpuMask (NOT stopped — keeps scheduling benefits on host cores). Restored to full CPU set on VM stop.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.vfio.sessionGpuDevices`

DRM device paths for KWIN_DRM_DEVICES. First device is primary render GPU —
set iGPU first for safe GPU passthrough. null = KWin auto-detects (unsafe).

WARNING: KWIN_DRM_DEVICES uses ':' as separator. Do NOT use /dev/dri/by-path/
paths — they contain ':' in the PCI address and will be split into garbage,
causing KWin to fail with "No suitable DRM devices have been found" and
trigger an SDDM infinite-login loop. Use /dev/dri/cardN instead.


- **Type:** `null or (list of string)`
- **Default:** `null`

### `myModules.vfio.staticPciIds`

PCI vendor:device IDs for static vfio-pci binding (e.g. ["1002:7550" "1002:ab40"])

- **Type:** `list of string`
- **Default:** `[]`

### `myModules.vfio.stealth._kernelPostPatch`

Combined kernel postPatch script. Apply via kernel overrideAttrs in host config.

- **Type:** `string`
- **Default:** `"  echo "=== BetterTiming: TSC compensation patch ==="

  # ---------- 1. Add tracking fields to struct kvm_vcpu ----------
  # Insert last_exit_start and total_exit_time after valid_wakeup
  if grep -q 'bool valid_wakeup;' include/linux/kvm_host.h; then
    sed -i '/bool valid_wakeup;/a\\n\tu64 last_exit_start;\n\tu64 total_exit_time;' \
      include/linux/kvm_host.h
    echo "[OK] kvm_host.h: added timing fields to struct kvm_vcpu"
  else
    echo "[FAIL] kvm_host.h: could not find valid_wakeup field"
    exit 1
  fi

  # ---------- 2. Rename vcpu_enter_guest → vcpu_enter_guest_real ----------
  if grep -q '^static int vcpu_enter_guest(struct kvm_vcpu \*vcpu)' arch/x86/kvm/x86.c; then
    sed -i 's/^static int vcpu_enter_guest(struct kvm_vcpu \*vcpu)/static int vcpu_enter_guest_real(struct kvm_vcpu *vcpu)/' \
      arch/x86/kvm/x86.c
    echo "[OK] x86.c: renamed vcpu_enter_guest to vcpu_enter_guest_real"
  else
    echo "[FAIL] x86.c: could not find vcpu_enter_guest definition"
    exit 1
  fi

  # ---------- 3. Add wrapper vcpu_enter_guest after vcpu_enter_guest_real ----------
  # Insert after the closing brace of vcpu_enter_guest_real, which is the first
  # solo '}' on a line after the function. We find it by locating the next function
  # (kvm_vcpu_running) and inserting before it.
  if grep -q '^static bool kvm_vcpu_running(struct kvm_vcpu \*vcpu)' arch/x86/kvm/x86.c; then
    sed -i '/^static bool kvm_vcpu_running(struct kvm_vcpu \*vcpu)/i\
static int vcpu_enter_guest(struct kvm_vcpu *vcpu)\
{\
\tint result;\
\tu64 difference;\
\
\tvcpu->last_exit_start = rdtsc();\
\
\tresult = vcpu_enter_guest_real(vcpu);\
\
\tif (vcpu->run->exit_reason == 123) {\
\t\tdifference = rdtsc() - vcpu->last_exit_start;\
\t\tvcpu->total_exit_time += difference;\
\t}\
\
\treturn result;\
}\
' arch/x86/kvm/x86.c
    echo "[OK] x86.c: added vcpu_enter_guest wrapper"
  else
    echo "[FAIL] x86.c: could not find kvm_vcpu_running to insert wrapper before"
    exit 1
  fi

  # ---------- 4. Patch MSR_IA32_TSC read to return compensated time ----------
  # Replace the TSC computation block in kvm_get_msr_common.
  # In 6.19, the MSR_IA32_TSC case has a block starting with:
  #   u64 offset, ratio;
  # We replace the entire case body with our compensated read.
  if grep -q 'case MSR_IA32_TSC: {' arch/x86/kvm/x86.c; then
    # Use awk for multi-line replacement within the MSR_IA32_TSC case
    awk '
    /case MSR_IA32_TSC: \{/ {
      print "\tcase MSR_IA32_TSC: {"
      print "\t\tu64 bt_diff;"
      print "\t\tu64 bt_total;"
      print ""
      print "\t\tbt_diff = rdtsc() - vcpu->last_exit_start;"
      print "\t\tbt_total = vcpu->total_exit_time + bt_diff;"
      print ""
      print "\t\tmsr_info->data = rdtsc() - bt_total;"
      print ""
      print "\t\tvcpu->run->exit_reason = 123;"
      print "\t\tbreak;"
      print "\t}"
      # Skip old content until the closing brace+break
      in_tsc = 1
      next
    }
    in_tsc && /^\tcase / { in_tsc = 0; print; next }
    in_tsc && /^\t\}/ { in_tsc = 0; next }
    in_tsc { next }
    { print }
    ' arch/x86/kvm/x86.c > arch/x86/kvm/x86.c.tmp && \
      mv arch/x86/kvm/x86.c.tmp arch/x86/kvm/x86.c
    echo "[OK] x86.c: patched MSR_IA32_TSC to return compensated time"
  else
    echo "[WARN] x86.c: could not find MSR_IA32_TSC case block — skipping"
  fi

  # ---------- 5. Enable RDTSC interception in init_vmcb ----------
  if grep -q 'svm_set_intercept(svm, INTERCEPT_RSM);' arch/x86/kvm/svm/svm.c; then
    sed -i '/svm_set_intercept(svm, INTERCEPT_RSM);/a\\tsvm_set_intercept(svm, INTERCEPT_RDTSC);' \
      arch/x86/kvm/svm/svm.c
    echo "[OK] svm.c: enabled RDTSC interception in init_vmcb"
  else
    echo "[WARN] svm.c: could not find INTERCEPT_RSM anchor for RDTSC interception"
  fi

  # ---------- 6. Add handle_rdtsc_interception handler ----------
  # Insert before the svm_exit_handlers table definition.
  # In 6.19, handlers take (struct kvm_vcpu *vcpu), and we use to_svm() to get svm.
  if grep -q '^static int (\*const svm_exit_handlers\[\])' arch/x86/kvm/svm/svm.c; then
    sed -i '/^static int (\*const svm_exit_handlers\[\])/i\
static int handle_rdtsc_interception(struct kvm_vcpu *vcpu)\
{\
\tstruct vcpu_svm *svm = to_svm(vcpu);\
\tu64 difference;\
\tu64 final_time;\
\tu64 data;\
\
\tdifference = rdtsc() - vcpu->last_exit_start;\
\tfinal_time = vcpu->total_exit_time + difference;\
\
\tdata = rdtsc() - final_time;\
\
\tvcpu->arch.regs[VCPU_REGS_RAX] = data & -1u;\
\tvcpu->arch.regs[VCPU_REGS_RDX] = (data >> 32) & -1u;\
\
\tvcpu->run->exit_reason = 123;\
\n\treturn kvm_skip_emulated_instruction(vcpu);\
}\
' arch/x86/kvm/svm/svm.c
    echo "[OK] svm.c: added handle_rdtsc_interception handler"
  else
    echo "[FAIL] svm.c: could not find svm_exit_handlers table"
    exit 1
  fi

  # ---------- 7. Register RDTSC handler in svm_exit_handlers table ----------
  # Add [SVM_EXIT_RDTSC] entry after [SVM_EXIT_AVIC_UNACCELERATED_ACCESS]
  if grep -q 'SVM_EXIT_AVIC_UNACCELERATED_ACCESS.*avic_unaccelerated_access_interception' arch/x86/kvm/svm/svm.c; then
    sed -i '/\[SVM_EXIT_AVIC_UNACCELERATED_ACCESS\].*=.*avic_unaccelerated_access_interception/a\\t[SVM_EXIT_RDTSC]\t\t\t\t= handle_rdtsc_interception,' \
      arch/x86/kvm/svm/svm.c
    echo "[OK] svm.c: registered SVM_EXIT_RDTSC handler"
  else
    echo "[WARN] svm.c: could not find AVIC_UNACCELERATED_ACCESS entry for RDTSC registration"
  fi

  # ---------- 8. Tag exit_reason=123 on CPUID, WBINVD, XSETBV, INVD ----------
  # In 6.19, these map directly to kvm_emulate_* functions. We create wrapper
  # functions that set exit_reason=123 then call through, and update the table.

  # 8a. Create wrapper functions (insert before svm_exit_handlers table)
  sed -i '/^static int handle_rdtsc_interception/i\
static int stealth_cpuid_interception(struct kvm_vcpu *vcpu)\
{\
\tvcpu->run->exit_reason = 123;\
\treturn kvm_emulate_cpuid(vcpu);\
}\
\
static int stealth_wbinvd_interception(struct kvm_vcpu *vcpu)\
{\
\tvcpu->run->exit_reason = 123;\
\treturn kvm_emulate_wbinvd(vcpu);\
}\
\
static int stealth_xsetbv_interception(struct kvm_vcpu *vcpu)\
{\
\tvcpu->run->exit_reason = 123;\
\treturn kvm_emulate_xsetbv(vcpu);\
}\
\
static int stealth_invd_interception(struct kvm_vcpu *vcpu)\
{\
\tvcpu->run->exit_reason = 123;\
\treturn kvm_emulate_invd(vcpu);\
}\
' arch/x86/kvm/svm/svm.c
  echo "[OK] svm.c: created stealth wrapper functions for exit_reason tagging"

  # 8b. Replace handler table entries to use wrappers
  sed -i 's/\[SVM_EXIT_CPUID\].*=.*kvm_emulate_cpuid,/[SVM_EXIT_CPUID]\t\t\t= stealth_cpuid_interception,/' \
    arch/x86/kvm/svm/svm.c
  sed -i 's/\[SVM_EXIT_WBINVD\].*=.*kvm_emulate_wbinvd,/[SVM_EXIT_WBINVD]\t\t\t= stealth_wbinvd_interception,/' \
    arch/x86/kvm/svm/svm.c
  sed -i 's/\[SVM_EXIT_XSETBV\].*=.*kvm_emulate_xsetbv,/[SVM_EXIT_XSETBV]\t\t\t= stealth_xsetbv_interception,/' \
    arch/x86/kvm/svm/svm.c
  sed -i 's/\[SVM_EXIT_INVD\].*=.*kvm_emulate_invd,/[SVM_EXIT_INVD]\t\t\t\t= stealth_invd_interception,/' \
    arch/x86/kvm/svm/svm.c
  echo "[OK] svm.c: updated exit handler table to use stealth wrappers"

  echo "=== BetterTiming: patch complete ==="
echo "=== CPUID Spoofing: Hypervisor-Phantom style patch ==="

# ---------- 1. Add reenter_guest_fast label before svm_vcpu_enter_exit ----------
# The call site in svm_vcpu_run looks like:
#   svm_vcpu_enter_exit(vcpu, spec_ctrl_intercepted);
# We add a label before it so we can goto from the CPUID handler.
if grep -q 'svm_vcpu_enter_exit(vcpu, spec_ctrl_intercepted);' arch/x86/kvm/svm/svm.c; then
  sed -i '/svm_vcpu_enter_exit(vcpu, spec_ctrl_intercepted);/i\\nreenter_guest_fast:' \
    arch/x86/kvm/svm/svm.c
  echo "[OK] svm.c: added reenter_guest_fast label"
else
  echo "[FAIL] svm.c: could not find svm_vcpu_enter_exit call site"
  exit 1
fi

# ---------- 2. Add CPUID leaf 0 spoofing after svm_vcpu_enter_exit ----------
# Insert the interception block between svm_vcpu_enter_exit() and the
# spec_ctrl_restore_host call. We anchor on the line after the enter_exit call.
#
# The code checks if exit_code == SVM_EXIT_CPUID and leaf (RAX) == 0.
# If so, it spoofs AuthenticAMD vendor string, caps max leaf at 0x16,
# advances RIP, and jumps back to re-enter the guest.
if grep -q 'svm_vcpu_enter_exit(vcpu, spec_ctrl_intercepted);' arch/x86/kvm/svm/svm.c; then
  sed -i '/^\tsvm_vcpu_enter_exit(vcpu, spec_ctrl_intercepted);$/a\\n\t/*\n\t * CPUID leaf 0 spoofing — Hypervisor-Phantom technique.\n\t * Intercept at lowest level, spoof vendor, re-enter without full exit.\n\t * Required kernel cmdline: mitigations=off idle=poll processor.max_cstate=1 tsc=reliable\n\t */\n\tif (unlikely(svm->vmcb->control.exit_code == SVM_EXIT_CPUID)) {\n\t\tif (svm->vmcb->save.rax == 0) {\n\t\t\tsvm->vmcb->save.rax = 0x16;\n\n\t\t\tvcpu->arch.regs[VCPU_REGS_RBX] = 0x68747541; /* Auth */\n\t\t\tvcpu->arch.regs[VCPU_REGS_RCX] = 0x444d4163; /* cAMD */\n\t\t\tvcpu->arch.regs[VCPU_REGS_RDX] = 0x69746e65; /* enti */\n\n\t\t\t{\n\t\t\t\tu64 next_rip = svm->vmcb->control.next_rip;\n\t\t\t\tif (!next_rip)\n\t\t\t\t\tnext_rip = svm->vmcb->save.rip + svm->vmcb->control.insn_len;\n\t\t\t\tsvm->vmcb->save.rip = next_rip;\n\t\t\t\tvcpu->arch.regs[VCPU_REGS_RIP] = next_rip;\n\t\t\t}\n\n\t\t\tgoto reenter_guest_fast;\n\t\t}\n\t}' arch/x86/kvm/svm/svm.c
  echo "[OK] svm.c: added CPUID leaf 0 spoofing with reenter_guest_fast"
else
  echo "[FAIL] svm.c: could not find svm_vcpu_enter_exit for CPUID insertion"
  exit 1
fi

# ---------- 3. Clear RDTSC/RDTSCP interception in init_vmcb ----------
# This ensures the guest can read TSC directly. When BetterTiming is also
# applied, its INTERCEPT_RDTSC set call (added after INTERCEPT_RSM) overrides
# this clear since it runs later in init_vmcb.
if grep -q 'svm_set_intercept(svm, INTERCEPT_RSM);' arch/x86/kvm/svm/svm.c; then
  sed -i '/svm_set_intercept(svm, INTERCEPT_RSM);/a\\tsvm_clr_intercept(svm, INTERCEPT_RDTSC);\n\tsvm_clr_intercept(svm, INTERCEPT_RDTSCP);' \
    arch/x86/kvm/svm/svm.c
  echo "[OK] svm.c: cleared RDTSC/RDTSCP interception in init_vmcb"
else
  echo "[WARN] svm.c: could not find INTERCEPT_RSM anchor for clearing RDTSC"
fi

echo "=== CPUID Spoofing: patch complete ==="
"`

### `myModules.vfio.stealth.acpiOem.id`

6-char ACPI OEM ID (padded with spaces). Defeats ACPI table header OEM ID checks.

- **Type:** `string`
- **Default:** `"ALASKA"`

### `myModules.vfio.stealth.acpiOem.tableId`

8-char ACPI OEM Table ID (padded with spaces). Defeats ACPI table header OEM Table ID checks.

- **Type:** `string`
- **Default:** `"A M I   "`

### `myModules.vfio.stealth.acpiSsdt.fakeBattery`

Include a fake ACPI battery device in the SSDT table. Defeats 'no battery = server/VM' heuristic.

- **Type:** `boolean`
- **Default:** `true`

### `myModules.vfio.stealth.acpiSsdt.sensorProbes`

Include CPU and VRM thermal zone probes (defeats MSAcpi_ThermalZoneTemperature WMI detection).

- **Type:** `boolean`
- **Default:** `true`

### `myModules.vfio.stealth.acpiSsdt.spoofedDevices`

Include spoofed ACPI device entries in the SSDT table. Defeats ACPI namespace enumeration checks.

- **Type:** `boolean`
- **Default:** `true`

### `myModules.vfio.stealth.aperfMperf`

Pass through IA32_APERF/MPERF MSRs to guest (defeats IET-based VM detection). Requires kernel 6.18+.

- **Type:** `boolean`
- **Default:** `true`

### `myModules.vfio.stealth.cpuIdentity.currentSpeed`

CPU current speed in MHz for SMBIOS type 4. null = omit.

- **Type:** `null or signed integer`
- **Default:** `null`

### `myModules.vfio.stealth.cpuIdentity.maxSpeed`

CPU max speed in MHz for SMBIOS type 4. null = omit.

- **Type:** `null or signed integer`
- **Default:** `null`

### `myModules.vfio.stealth.cpuIdentity.modelId`

CPU model string reported to guest (e.g. 'AMD Ryzen 7 7800X3D 8-Core Processor'). null = use host CPU.

- **Type:** `null or string`
- **Default:** `null`

### `myModules.vfio.stealth.cpuidSpoof.enable`

CPUID leaf 0 spoofing via Hypervisor-Phantom technique. Defeats anti-cheat that checks the hypervisor-present CPUID bit (leaf 1, bit 31) and hypervisor vendor string (leaf 0x40000000).

- **Type:** `boolean`
- **Default:** `true`

### `myModules.vfio.stealth.disk.model`

Emulated disk model string (24 chars padded with spaces). Defeats Win32_DiskDrive.Model checks.

- **Type:** `string`
- **Default:** `"WDC WD10EZEX-00W          "`

### `myModules.vfio.stealth.disk.opticalModel`

Emulated optical drive model (24 chars padded). Defeats Win32_CDROMDrive.Name checks.

- **Type:** `string`
- **Default:** `"HL-DT-ST DVDRAM GH24NSC0  "`

### `myModules.vfio.stealth.edid.dpi`

Monitor DPI.

- **Type:** `signed integer`
- **Default:** `91`

### `myModules.vfio.stealth.edid.manufacturer`

3-char EDID manufacturer code.

- **Type:** `string`
- **Default:** `"ACI"`

### `myModules.vfio.stealth.edid.model`

16-char padded EDID model string.

- **Type:** `string`
- **Default:** `"ASUS VG248      "`

### `myModules.vfio.stealth.edid.modelAbbrev`

8-char padded EDID model abbreviation.

- **Type:** `string`
- **Default:** `"ACI     "`

### `myModules.vfio.stealth.edid.productCode`

EDID product code (hex).

- **Type:** `string`
- **Default:** `"0x2480"`

### `myModules.vfio.stealth.edid.serial`

Monitor serial string.

- **Type:** `string`
- **Default:** `"VG248QE"`

### `myModules.vfio.stealth.edid.week`

EDID manufacture week.

- **Type:** `signed integer`
- **Default:** `22`

### `myModules.vfio.stealth.edid.year`

EDID manufacture year.

- **Type:** `signed integer`
- **Default:** `2020`

### `myModules.vfio.stealth.enable`

Whether to enable VFIO stealth anti-detection stack.

- **Type:** `boolean`
- **Default:** `false`

### `myModules.vfio.stealth.hypervVendorId`

Hyper-V vendor_id reported to guest (1–12 chars, libvirt hard limit). Avoid well-known VM values like 'AMDisbetter!' or 'Microsoft Hv'.

- **Type:** `string matching the pattern ^.{1,12}$`
- **Default:** `"AuthAMDRyzen"`

### `myModules.vfio.stealth.kernelParams.maxCState`

processor.max_cstate value passed on the kernel command line. Lower values reduce timing jitter that can reveal virtualization.

- **Type:** `signed integer`
- **Default:** `1`

### `myModules.vfio.stealth.kernelParams.tscReliable`

Pass tsc=reliable on the kernel command line. Prevents kernel from falling back to HPET/ACPI PM timer, which anti-cheat can detect.

- **Type:** `boolean`
- **Default:** `true`

### `myModules.vfio.stealth.macPrefix`

OUI prefix used when spoofMac is enabled (colon-separated hex, e.g. 00:1b:21). Intel OUI by default.

- **Type:** `string`
- **Default:** `"00:1b:21"`

### `myModules.vfio.stealth.smbios.biosVendor`

SMBIOS BIOS vendor string. Defeats WMI Win32_BIOS.Manufacturer checks.

- **Type:** `string`
- **Default:** `"American Megatrends Inc."`

### `myModules.vfio.stealth.smbios.biosVersion`

SMBIOS BIOS version string. Defeats WMI Win32_BIOS.SMBIOSBIOSVersion checks.

- **Type:** `string`
- **Default:** `"1001"`

### `myModules.vfio.stealth.smbios.cache.l1`

L1 cache size in KB for SMBIOS type 7.

- **Type:** `signed integer`
- **Default:** `512`

### `myModules.vfio.stealth.smbios.cache.l2`

L2 cache size in KB for SMBIOS type 7.

- **Type:** `signed integer`
- **Default:** `8192`

### `myModules.vfio.stealth.smbios.cache.l3`

L3 cache size in KB for SMBIOS type 7.

- **Type:** `signed integer`
- **Default:** `32768`

### `myModules.vfio.stealth.smbios.manufacturer`

SMBIOS system manufacturer string. Defeats WMI Win32_ComputerSystem.Manufacturer checks.

- **Type:** `string`
- **Default:** `"To Be Filled By O.E.M."`

### `myModules.vfio.stealth.smbios.memory.count`

Number of DIMMs to report.

- **Type:** `signed integer`
- **Default:** `2`

### `myModules.vfio.stealth.smbios.memory.manufacturer`

DIMM manufacturer for SMBIOS type 17. Defeats WMI Win32_PhysicalMemory.Manufacturer checks.

- **Type:** `string`
- **Default:** `"Kingston"`

### `myModules.vfio.stealth.smbios.memory.partNumber`

DIMM part number. Defeats WMI Win32_PhysicalMemory.PartNumber checks.

- **Type:** `string`
- **Default:** `"KF560C36-16"`

### `myModules.vfio.stealth.smbios.memory.size`

DIMM size in MB (per module).

- **Type:** `signed integer`
- **Default:** `16384`

### `myModules.vfio.stealth.smbios.memory.speed`

Memory speed in MT/s. Defeats WMI Win32_PhysicalMemory.Speed checks.

- **Type:** `signed integer`
- **Default:** `4800`

### `myModules.vfio.stealth.smbios.product`

SMBIOS product name string. Defeats WMI Win32_ComputerSystem.Model checks.

- **Type:** `string`
- **Default:** `"To Be Filled By O.E.M."`

### `myModules.vfio.stealth.smbios.serial`

SMBIOS system serial number. Defeats WMI Win32_ComputerSystemProduct.IdentifyingNumber checks.

- **Type:** `string`
- **Default:** `"System Serial Number"`

### `myModules.vfio.stealth.smbios.socketPrefix`

SMBIOS processor socket designator prefix (type 4). Defeats WMI Win32_Processor.SocketDesignation checks.

- **Type:** `string`
- **Default:** `"AM5"`

### `myModules.vfio.stealth.spoofMac`

Spoof guest NIC MAC address with a realistic OUI prefix. Defeats MAC OUI-based VM detection.

- **Type:** `boolean`
- **Default:** `true`

### `myModules.vfio.stealth.stripVirtio`

Remove VirtIO balloon, RNG, and tablet devices from VM config when stealth is enabled. Defeats PCI device enumeration checks.

- **Type:** `boolean`
- **Default:** `true`

### `myModules.vfio.stealth.timing.enable`

BetterTiming TSC compensation (hides VM exit timing from guests). Defeats timing-based VM detection that measures RDTSC deltas across CPUID/VMCALL.

- **Type:** `boolean`
- **Default:** `true`

### `myModules.vfio.vmDiskPath`

Directory for VM disk images and state files

- **Type:** `string`
- **Default:** `"/var/lib/vfio"`

### `myModules.vfio.vms`

Per-VM definitions

- **Type:** `lazy attribute set of (submodule)`
- **Default:** `{ … }`